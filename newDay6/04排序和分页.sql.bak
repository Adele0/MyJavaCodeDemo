-- 执行顺序 FROM --> WHERE --> SELECT --> ORDER BY
-- 结果排序OEDER BY出现在select语句最后 其中： ASC升 DESC降
SELECT emp_name,emp_id,salary FROM employee ORDER BY salary DESC;
-- W3School上有 FIRST() LAST()可以选第一位 最后一位，这里不可以？？

-- 分页 LIMIT 假分页（几乎不用）和真分页（数据在内容中有多少条，显示多少条）
-- 语法： SELECT * FROM TABLE_NAME LIMIT beginIndex(本页的开始的索引),pageSize;
SELECT * FROM employee LIMIT 0,3;
SELECT * FROM employee LIMIT 2,6;

